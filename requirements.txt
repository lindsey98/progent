# Dependencies for Progent (secagent) ONLY.
#
# secagent (the root package, i.e. "this repo itself") is NOT installed as a
# package. Instead, install just its dependencies here and run it directly from
# the repository directory:
#
#   pip install -r requirements.txt
#
# The benchmark harnesses ARE installed from their own directories, e.g.:
#
#   cd agentdojo && pip install -e .     # installs agentdojo + its deps
#
# When you run agentdojo/run.sh, it adds the repo root to PYTHONPATH so that
# `import secagent` resolves to this directory (no install needed).
#
# ASB and the real-world-agents harness have their own requirements files:
#   asb/requirements.txt, real-world-agents/requirements.txt

docstring-parser>=0.15
jsonschema
pydantic[email]>=2.7.1
openai>=1.59.7
anthropic>=0.47.0
z3-solver
