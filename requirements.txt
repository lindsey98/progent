# Dependencies for Progent (secagent) + the AgentDojo benchmark.
#
# This lets you install everything WITHOUT doing an editable install
# (`pip install -e .`). After `pip install -r requirements.txt`, run the code
# directly from the repo by putting the local packages on PYTHONPATH, e.g.:
#
#   export PYTHONPATH="$PWD/agentdojo/src:$PWD:$PYTHONPATH"
#   python -m agentdojo.scripts.benchmark -s banking --model gpt-4o-2024-08-06 --logdir logs
#
# (agentdojo/src must come before the repo root so the `agentdojo` package in
# src/ is found instead of the top-level agentdojo/ directory.)
#
# ASB and the real-world-agents harness have their own requirements files:
#   asb/requirements.txt, real-world-agents/requirements.txt

# --- Progent (secagent) core ---
docstring-parser>=0.15
jsonschema
pydantic[email]>=2.7.1
openai>=1.59.7
anthropic>=0.47.0
z3-solver

# --- AgentDojo benchmark ---
tenacity>=8.2.3
langchain>=0.1.17
typing-extensions>=4.11.0
cohere>=5.3.4
pyyaml>=6.0.1
click>=8.1.7
deepdiff>=7.0.1
rich>=13.7.1
vertexai>=1.49.0
openapi-pydantic>=0.5.0
jsonref>=1.1.0
python-dotenv>=1.0.1
