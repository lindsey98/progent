# Dependencies for the agentdojo-mcp tool/environment server (mcp_server.py).
#
# Install without an editable install, then run from the directory by putting
# this package's src/ on PYTHONPATH (before the repo root):
#
#   pip install -r requirements.txt
#   export PYTHONPATH="$PWD/src:$PWD/..:$PYTHONPATH"
#   python mcp_server.py

# --- AgentDojo (MCP variant) ---
openai>=1.59.7
pydantic[email]>=2.7.1
docstring-parser>=0.15
tenacity>=8.2.3
langchain>=0.1.17
typing-extensions>=4.11.0
anthropic>=0.47.0
cohere>=5.3.4
pyyaml>=6.0.1
click>=8.1.7
deepdiff>=7.0.1
rich>=13.7.1
vertexai>=1.49.0
openapi-pydantic>=0.5.0
jsonref>=1.1.0
python-dotenv>=1.0.1

# --- MCP server ---
fastmcp~=2.13.0
fastapi>=0.120
uvicorn

# --- Progent (secagent) core, needed by the proxy/wrappers ---
jsonschema
z3-solver
